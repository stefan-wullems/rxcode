import {render, screen} from '@testing-library/svelte'

import Margin from './margin.svelte'

describe('The Margin component', () => {
  it.todo('renders the amount of rows specified in the props')
  it.todo('creates an instance of the columns in each row')
  it.todo('passes the row number to the columns')
})
